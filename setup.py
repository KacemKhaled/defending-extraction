import setuptools


setuptools.setup(
    name="df",
    version="0.1",
    author="Anonymous",
    packages=setuptools.find_packages(),
    # packages=setuptools.find_packages(include=['df', 'df.*']),
    classifiers=[
        "Programming Language :: Python :: 3"
    ],
)